# IrtrixAI Command Line Interface (CLI v1)

The IrtrixAI CLI is a lightweight, dependency-isolated terminal client for the IrtrixAI Autonomous Coding Assistant. It interacts exclusively with the FastAPI backend through HTTP.

---

## 1. Installation

Install the CLI in editable mode inside your development environment:

```powershell
pip install -e cli/
```
